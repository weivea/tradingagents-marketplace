[project]
name = "paper-trading-mcp"
version = "0.1.0"
description = "Multi-account simulated trading engine"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
