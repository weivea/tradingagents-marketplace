{
  "name": "gv",
  "version": "0.1.0",
  "description": "MCP server for generating narrated scrolling videos from Chinese analysis reports",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "setup:python": "uv sync --project . && uv run --project . playwright install chromium",
    "setup:remotion": "cd remotion && npm install",
    "setup": "npm install && npm run build && npm run setup:python && npm run setup:remotion"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
